# DEPLOYMENT.md — قائمة ما قبل الإنتاج (MUTQEN)

هذا الملف يلخّص ما يجب فعله قبل تشغيل النظام ببيانات حقيقية، وما يخصّ التطوير المحلي.

## 1) قائمة ما قبل الإنتاج (إلزامية)

| # | البند | كيف |
|---|---|---|
| 1 | إطفاء وضع التصحيح | `.env`: `APP_DEBUG=false` و`APP_ENV=production` (يمنع تسريب آثار الأخطاء؛ ومعه لا يمكن أن يظهر `dev_otp` في أي استجابة — الشرط أصلاً `environment('local')`) |
| 2 | مفتاح تطبيق فريد | `php artisan key:generate` على الخادم (لا تنسخ مفتاح التطوير) |
| 3 | تقييد CORS | `config/cors.php`: استبدل `allowed_origins => ['*']` بنطاق الواجهة الفعلي فقط (مثل `https://mutqin.example.ly`) |
| 4 | بوابة SMS للـ OTP | نقطة الدمج الوحيدة: `AuthController::sendOtp()` — اربطها بمزوّد (ليبيانا/مدار). بدونها لا يمكن لولي الأمر/المحفّظ استعادة كلمة المرور ذاتياً في الإنتاج |
| 5 | التشغيل عبر Apache/nginx | لا تستخدم `php artisan serve` في الإنتاج (أحادي الخيط). وجّه DocumentRoot إلى `backend/public`، وقدّم `frontend-html/` كملفات ساكنة، وحدّث `frontend-html/js/config.js` (`API_BASE_URL`) |
| 6 | نسخ احتياطي لقاعدة البيانات | جدولة `mysqldump` يومياً على الأقل لـ `mutqin_db` (بيانات قُصّر — لا تفريط). مثال: `mysqldump -u root mutqin_db > backup-$(date +%F).sql` |
| 7 | HTTPS | شهادة TLS إلزامية — التوكنات تُرسل في ترويسة Authorization |
| 8 | حسابات تجريبية | غيّر كلمات مرور حسابات الـ seeder أو احذفها (`password` معلنة في صفحة الدخول أثناء التطوير؛ لوحة «بيانات تجريبية» تختفي تلقائياً خارج dev لكن الحسابات تبقى) |
| 9 | الاختبارات خضراء | `php artisan test` قبل كل نشر (تغطي مصفوفة الأدوار والملكية وOTP والحدّ) |

## 2) إعدادات مطبّقة مسبقاً (لا تُرجِعها للخلف)

- **المنطقة الزمنية**: `Africa/Tripoli` في `config/app.php` — منطق «اليوم» كله يعتمد عليها.
- **صلاحية التوكنات**: 7 أيام (`config/sanctum.php`)، وتُبطل كلها عند تغيير كلمة المرور.
- **CORS credentials**: `supports_credentials => false` (مصادقة Bearer لا كوكيز).
- **حدّ محاولات**: الدخول 10/دقيقة، طلب OTP 5/دقيقة، تحقّق OTP 10/دقيقة.

## 3) التطوير المحلي

- **خادم PHP المدمج أحادي الخيط** — الطلبات المتزامنة تتسلسل (تلمسه في القوائم التابعة والـ polling). الحل:
  ```bat
  set PHP_CLI_SERVER_WORKERS=4
  C:\xampp\php\php.exe artisan serve --port=9090
  ```
  أو استخدم Apache من XAMPP مباشرة.
- إضافتا PHP المطلوبتان في `php.ini`: `zip` (استيراد xlsx) و`gd` (mPDF).
- قاعدة الاختبار: `mutqin_test` (تُدار تلقائياً عبر `php artisan test` — لا تستخدمها لبيانات فعلية).
- الـ OTP في التطوير: يظهر في استجابة الطلب (`dev_otp`) وفي `storage/logs/laravel.log` — بيئة `local` فقط.

## 4) ملاحظات مستقبلية (غير حاجزة)

- إضافة soft-deletes/سجل تدقيق للكيانات الحسّاسة (طلاب/مستخدمون).
- توحيد شكل ردود `?all=1` مقابل المرقّمة (الواجهة تتعامل مع الشكلين حالياً).
- فهرس فريد للهاتف على مستوى القاعدة غير ممكن «ضمن الدور» في MariaDB 10.4 — التفرّد مضمون منطقياً عبر `ParentResolver` ومغطّى بالاختبارات.
